import request from 'superagent';

import phones from './mockPhones';
import categories from './mockCategories';

let SERVER = 'http://localhost:3012';

/**
 * Receives weather forecast from API
 * @param zip - city code
 * @param units
 * @returns {Promise<void>}
 */
export const fetchWeathers = async (zip, units) => {
    return new Promise((resolve, reject ) => {
        let appId = 'b1b35bba8b434a28a0be2a3e1071ae5b';
        const URL =
            "http://api.openweathermap.org/data/2.5/weather?q=" +
            zip + "&appid=" + appId + "&units=" + units;

        fetch(URL).then(res => res.json()).then(json => {
            resolve (json);
        });
    })


};

export const fetchPhones = async () => {
    const {body} = await request.get(
        // 'http://www.mocky.io/v2/5b32193b3400005d003fd26b'
        SERVER + '/phones'
    );
    return body.phones
    // return new Promise(resolve  => {
    //       resolve(phones);
    // })
};

export const loadMorePhones = async ({offset}) => {
    return new Promise(resolve => {
        resolve(phones);
    })
};

export const fetchPhoneById = async id => { //return one object
    const {body} = await request.get(
        SERVER + '/phone/' + id
    );
    return body

    // return new Promise((resolve, reject) => {
    //
    //     const phone = R.find(R.propEq('id', id), phones);
    //     resolve(phone);
    // })
};

export const fetchCategories = async () => {
    return new Promise(resolve => {
        resolve(categories)
    })
};